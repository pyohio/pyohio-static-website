[build]
  publish = "public"
  command = "pip install rockgarden && curl --proto '=https' --tlsv1.2 -sSf https://just.systems/install.sh | bash -s -- --to /opt/build/repo/bin && PATH=\"$PATH:/opt/build/repo/bin\" just build-prod"

[build.environment]
  PYTHON_VERSION = "3.12"
